# M0 Linear Seeding Pack

最后更新：2026-06-28

这个文件用于把 `M0 · 文档与产品定义冻结` 从“只有 milestone 描述”补成可统计、可追溯的已完成 issue 集合。

## M0 目标

在进入真正实现前，先把 AI 协作底座的产品定位、输入输出、客户端适配范围、UI/UX 参考与高保真实现路径定义清楚。

## M0 对应 PRD 交付

- PRD + Roadmap
- 用户输入输出说明
- P0 信息架构
- 客户端适配清单
- UI/UX 参考图选择结论
- 高保真还原操作流程

## 建议补建的已完成 Issues

1. `M0-01 PRD 与 Roadmap 冻结`
2. `M0-02 输入输出与客户端适配定义`
3. `M0-03 UI/UX 参考与高保真实现流程冻结`

## 建议状态

- `M0-01`: Done
- `M0-02`: Done
- `M0-03`: Done

## 为什么现在补建 M0

- `M0` 的工作实际已经完成，但此前没有被 issue 化
- Linear 的 milestone 进度只认 issue，不认描述文本
- 补建后，`M0` 的完成状态会和 `M1`、`M2` 一样可见、可统计、可审计
